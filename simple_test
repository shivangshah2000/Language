import std::borrow::Cow;

mod lexer {
    struct Lexer {
        input: &[u8],
        tokens: VecToken,
    }

    fn new_lexer(input: &[u8]) -> Self {
        Self {
            input: input, tokens: VecToken::new()
        }
    }

    fn lex(&mut self) -> VecToken {
        
    }
}